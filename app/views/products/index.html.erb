<h1>Products</h1>

<% @products.each do |product|%>
    <h1> <%= product.name %> </h1>
    <p> <%= product.description %> </p>
        <strong> Price: </strong>
        <%= number_to_currency(product.price, locale: :pk) %>
<% end %>
